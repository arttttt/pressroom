{
	// A solution file: it holds no sources of its own, it only points editors at
	// the two real projects. `pnpm typecheck` runs both.
	"files": [],
	"references": [{ "path": "./tsconfig.node.json" }, { "path": "./tsconfig.web.json" }]
}
